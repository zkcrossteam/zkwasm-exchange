// Import and re-export the Event model from zkwasm-ts-server
// Make sure to include the .js extension in import paths (required for ES modules)
import { Event, EventModel } from 'zkwasm-ts-server/src/event.js';

export { Event, EventModel };
